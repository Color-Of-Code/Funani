<UserControl x:Class="Funani.Gui.Controls.MongoDbView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    	<TabControl>
    		<TabItem Header="Console">
		        <ListView Name="console" ItemsSource="{Binding Lines}">
		        </ListView>
    		</TabItem>
    		<TabItem Header="Query">
    			<Grid>
    				<Grid.ColumnDefinitions>
    					<ColumnDefinition></ColumnDefinition>
    					<ColumnDefinition Width="Auto"></ColumnDefinition>
    				</Grid.ColumnDefinitions>
    				<Grid.RowDefinitions>
    					<RowDefinition Height="Auto"></RowDefinition>
    					<RowDefinition></RowDefinition>
    				</Grid.RowDefinitions>
	    			<TextBox Name="query" Text="{Binding Query}" Grid.Column="0" Grid.Row="0"></TextBox>
	    			<Button Name="run" Content="Run" Click="Run_Click" Grid.Column="1" Grid.Row="0"></Button>
			        <ListView Name="queryResult" ItemsSource="{Binding QueryResults}" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">
			        </ListView>
				</Grid>
    		</TabItem>
    	</TabControl>
    </Grid>
</UserControl>
